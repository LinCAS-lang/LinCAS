
module Test {
  const Letters := {
    success: "."
    fail:    "F"
    error:   "E"
  }

  let self.run_before_each_hooks {}
  let self.run_after_each_hooks {}

  at_exit {
    Test::Context.finish
  }
}